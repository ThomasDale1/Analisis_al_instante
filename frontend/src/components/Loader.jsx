/**
 * Loader con mensaje para mostrar mientras analiza la IA.
 */
const Loader = () => (
  <div style={{ margin: "2rem", fontWeight: "bold" }}>
    <span>🧠 Analizando tus datos con IA... por favor espera.</span>
  </div>
);

export default Loader;
